server:
  servlet:
    context-path: "/api/v1/auth"
  port: 8081

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: 728926
    url: jdbc:postgresql://localhost:5432/pet-user
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/pethealth
